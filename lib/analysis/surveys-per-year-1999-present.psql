COPY (
  SELECT extract(year from s.survey_date) AS year, COUNT(*) 
  FROM surveys s
	WHERE extract(year from s.survey_date) >= 1999 
		AND is_survey AND is_complete
  GROUP BY year ORDER BY year, count DESC
) TO '/home/scw/coasst/da/lib/analysis/surveys-per-year-1999-present.csv' WITH CSV HEADER;
